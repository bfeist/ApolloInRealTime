# Lifted legacy code (Phase 1+)

Code copied wholesale from the legacy mission repos lands here during the
lift phases. **This tree is excluded from ESLint and TypeScript checks**
by design — it is pre-existing JS that we will rewrite into `src/` module
by module, not lint-clean as-is.

As a file moves out to `src/`, it comes under the strict toolchain.
